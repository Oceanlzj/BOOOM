using Assets.BasicModule.Model;
using Assets.BasicModule.Utility;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestMain : MonoBehaviour
{
  // Start is called before the first frame update
  void Start()
  {

    //Dish d = new Dish(0,"Test", "TestDes", 10.2, 3.3, 20, 1.0, Dish.DishType.Normal,
    //  new List<FoodProperty>() { FoodProperty.Normal, FoodProperty.Acid});

    //JsonWriter<Dish>.Write(d, "test.json");

    //Dish a = JsonReader<Dish>.Read("Data\\Dish\\test.json");

    //string Path = "Recipe.json";
    //Recipe SingleRecipe = new Recipe();
    //SingleRecipe.Recipes.Add("1,2", 1);
    //JsonWriter<Recipe>.Write(SingleRecipe, Path);

    //Recipe recipe = JsonReader<Recipe>.Read("Data\\Recipe\\Recipe.json");
  }

  // Update is called once per frame
  void Update()
  {

  }
}
